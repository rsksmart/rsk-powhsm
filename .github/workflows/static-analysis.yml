name: "Static analysis"

on: [push]

# Declare default permissions as read only.
permissions: read-all

jobs:
  static-analysis:
    name: Run static analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout this repo
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0

      - name: Build the ledger docker image
        run: docker/ledger/build

      - name: Build the SGX docker image
        run: docker/sgx/build
      
      - name: Run Ledger static analysis
        id: ledger-static-analysis
        run: |
          firmware/static-analysis/gen-static-analysis ledger
      
      - name: Run SGX static analysis
        id: sgx-static-analysis
        continue-on-error: true
        run: |
          firmware/static-analysis/gen-static-analysis sgx
      
      - name: Upload static analysis reports
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #v4.6.2
        with:
          name: static-analysis-reports
          path: firmware/static-analysis/output

      - name: Report static analysis findings
        if: steps.ledger-static-analysis.outcome != 'success' || steps.sgx-static-analysis.outcome != 'success'
        run: |
          echo "Static analysis reported findings. Check static-analysis-reports for a complete report." \
            > $GITHUB_STEP_SUMMARY
